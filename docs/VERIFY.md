# How to Verify CLAW v0 (genesis-v0)

This guide verifies that a CLAW receipt/proof is internally consistent and (optionally) anchored.

## 1) Get the frozen protocol version

```bash
git clone <your-repo-url>
cd claw-bot
git checkout genesis-v0
