[pytest]
testpaths = backend/tests
addopts = -q -m "not slow"
markers =
    unit: isolated deterministic unit test
    invariant: protocol/lifecycle invariants (hashing, determinism, replay safety)
    e2e: end-to-end workflow/system test
    slow: long-running test excluded from default runs

