/**
 * Paid Pro review layout tokens — spacing between agreement paper and post-document guidance.
 */

/** Tail padding under agreement pre block (compact paid review). */
export const PAID_PRO_REVIEW_DOCUMENT_TAIL_PADDING_CLASS = "pb-3 sm:pb-4";

/** Legacy / non-compact paper tail padding. */
export const PAID_PRO_REVIEW_DOCUMENT_TAIL_PADDING_LEGACY_CLASS = "pb-12";

/** Vertical stack gap between document block and Review Status (compact). */
export const PAID_PRO_REVIEW_POST_DOCUMENT_STACK_GAP_CLASS = "gap-1.5";

/** Max spacer height applied between guidance and sticky CTA scroll tail (not before status). */
export const PAID_PRO_REVIEW_GUIDANCE_MAX_TOP_MARGIN_CLASS = "mt-0";
